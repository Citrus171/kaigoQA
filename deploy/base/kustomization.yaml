apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - configmap.yaml
  # secret は git 管理外。hybrid-secret を ns へ手動適用する（REPLACE_ME を ArgoCD に同期させない）。
  - api-deployment.yaml
  - api-service.yaml
  - web-deployment.yaml
  - web-service.yaml
  - postgres-statefulset.yaml
  - postgres-service.yaml
  - ollama-deployment.yaml
  - ollama-service.yaml
