apiVersion: v1
kind: Secret
metadata:
  name: hybrid-secret
type: Opaque
stringData:
  DATABASE_URL: "postgresql://<POSTGRES_USER>:<POSTGRES_PASSWORD>@postgres:5432/functest_hono"
  JWT_SECRET: "REPLACE_ME"
  OPENCODE_API_KEY: "REPLACE_ME"
  CF_ACCOUNT_ID: "REPLACE_ME"
  CF_API_TOKEN: "REPLACE_ME"
  OPENROUTER_API_KEY: "REPLACE_ME"
  # prod overlay が postgres StatefulSet の env をこれらに差し替える
  POSTGRES_USER: "REPLACE_ME"
  POSTGRES_PASSWORD: "REPLACE_ME"
